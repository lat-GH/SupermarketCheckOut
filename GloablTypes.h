#ifndef GLOBAL_TYPES
#define GLOBAL_TYPES

#include <vector>
#include <string>
#include <unordered_map>

using namespace std;

using CSV_table = vector<vector<string>>;
using CSV_row = unordered_map<string, string>;

#endif